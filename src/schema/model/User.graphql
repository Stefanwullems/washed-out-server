type User {
  fullName: String
  email: String
  password: String
  bio: String
  picture: String
  location: Location
  items: [Item]
  createdRequests: [ServiceRequest]
  recievedRequests: [ServiceRequest]
  services: Services
  token: String
}
