type User {
  id: Int!
  fullName: String!
  email: String!
  password: String!
  bio: String
  picture: String
  location: Location
  createdRequests: [ServiceRequest]
  recievedRequests: [ServiceRequest]
  services: Services!
  token: String
  serviceFees: ServiceFees
}
