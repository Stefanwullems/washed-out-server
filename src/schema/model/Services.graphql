type Services {
  id: Int!
  washing: Boolean!
  drying: Boolean!
  ironing: Boolean!
  folding: Boolean!
  pickup: Boolean!
  delivery: Boolean!
}

input ServicesInput {
  washing: Boolean
  drying: Boolean
  ironing: Boolean
  folding: Boolean
  pickup: Boolean
  delivery: Boolean
}
