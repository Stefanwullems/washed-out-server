type Mutation {
  setLocation(
    userId: Int!
    address: String!
    postalCode: String!
    city: String!
    country: String!
  ): Location
}

type Query {
  getLocation(userId: Int!): Location
}
